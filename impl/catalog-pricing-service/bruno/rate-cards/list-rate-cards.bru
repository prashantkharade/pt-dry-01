meta {
  name: List Rate Cards
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/rate-cards
  body: none
  auth: inherit
}

tests {
  test("List rate cards works", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
