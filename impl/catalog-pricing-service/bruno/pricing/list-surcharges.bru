meta {
  name: List Surcharges
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/pricing/surcharges
  body: none
  auth: inherit
}

tests {
  test("List surcharges works", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
