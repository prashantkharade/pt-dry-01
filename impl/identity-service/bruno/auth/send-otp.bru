meta {
  name: Send OTP
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/otp/send
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "Phone": "+919876543210"
  }
}

tests {
  test("Send OTP works", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
