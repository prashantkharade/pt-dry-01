meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: none
  auth: inherit
}

tests {
  test("Logout works", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
