meta {
  name: Get Customer By Id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/customers/{{CUSTOMER_ID}}
  body: none
  auth: inherit
}

tests {
  test("Get Customer By Id works", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
