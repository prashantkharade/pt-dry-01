meta {
  name: Get User By Id
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/users/{{USER_ID}}
  body: none
  auth: inherit
}

tests {
  test("Get User By Id works", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
