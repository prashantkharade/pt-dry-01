meta {
  name: List Branches
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/tenants/branches
  body: none
  auth: inherit
}

tests {
  test("List Branches works", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
